#include "Session.hpp"
#include "File_header.hpp"
#include <fstream>
#include <iostream>
using namespace std;
int main()
{   
    string date, insights, positions;
    int rolls, passes, defences;

    cout << "date: ";
    cin >> date;

    cout << "insights: ";
    cin >> insights;

    //do-while to get positions

    //Session session(date, Positions({}, "abcd"), insights, Rolls(1,2,3));
    //session.create_file();
    return 0;

}
